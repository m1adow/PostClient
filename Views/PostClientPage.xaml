<Page
    x:Class="PostClient.PostClientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PostClient"
    xmlns:ineractivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:PostClient.ViewModels"
    xmlns:uc="using:PostClient.Views.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <vm:PostClientViewModel x:Key="viewModel"/>
    </Page.Resources>
    <ineractivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding Source={StaticResource viewModel}, Path=LoadedHandlerCommand}"/>
        </core:EventTriggerBehavior>
    </ineractivity:Interaction.Behaviors>
    <Grid DataContext="{StaticResource viewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10"
                    Grid.Column="0">
            <Button Content="Login"
                    Command="{Binding LoginViewModel.ShowLoginControlsCommand}"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"/>
            <Button Content="New mail"
                    Command="{Binding SendMessageViewModel.ShowSendingControlosCommand}"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"/>
            <TextBlock Text="Folders"
                       Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"/>
            <Button Content="Inbox"
                    Margin="10,5,0,0"
                    HorizontalAlignment="Left"
                    Width="80"
                    Height="40"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"
                    Command="{Binding LoadMessagesViewModel.LoadAllMessagesFromLocalStorageCommand}"/>
            <Button Content="Sent Mail"
                    Margin="10,5,0,0"
                    HorizontalAlignment="Left"
                    Width="80"
                    Height="40"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"
                    Command="{Binding LoadMessagesViewModel.LoadSentMessagesFromLocalStorageCommand}"/>
            <Button Content="Flagged"
                    Margin="10,5,0,0"
                    HorizontalAlignment="Left"
                    Width="80"
                    Height="40"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"
                    Command="{Binding LoadMessagesViewModel.LoadFlaggedMessagesFromLocalStorageCommand}"/>
            <Button Content="Drafts"
                    Margin="10,5,0,0"
                    HorizontalAlignment="Left"
                    Width="80"
                    Height="40"
                    Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"
                    Command="{Binding LoadMessagesViewModel.LoadDraftMessagesFromLocalStorageCommand}"/>
            <TextBlock Text="Login:"
                       Margin="0,10,0,0"
                       Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
            <TextBox Visibility="{Binding LoginViewModel.LoginControlsVisibility}"
                     Text="{Binding LoginViewModel.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Password:"
                       Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
            <TextBox Visibility="{Binding LoginViewModel.LoginControlsVisibility}"
                     Text="{Binding LoginViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Remember me"
                      Visibility="{Binding LoginViewModel.LoginControlsVisibility}"
                      IsChecked="{Binding LoginViewModel.IsRememberMeChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Select service:"
                       Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <RadioButton Grid.Column="0"
                             Content="Gmail" 
                             IsChecked="{Binding LoginViewModel.IsGmailRadioButtonChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
                <RadioButton Grid.Column="1" 
                             Content="Outlook"
                             IsChecked="{Binding LoginViewModel.IsOutlookRadioButtonChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Margin="5"
                        Content="OK"
                        Command="{Binding LoginViewModel.LoginCommand}"
                        Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
                <Button Grid.Column="1"
                        Margin="5"
                        Content="Cancel"
                        Command="{Binding LoginViewModel.CancelLoginControlsCommand}"
                        Visibility="{Binding LoginViewModel.LoginControlsVisibility}"/>
            </Grid>
        </StackPanel>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Search here:"/>
                    <TextBox Text="{Binding LoadMessagesViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <ineractivity:Interaction.Behaviors>
                            <core:EventTriggerBehavior EventName="TextChanged">
                                <core:InvokeCommandAction Command="{Binding LoadMessagesViewModel.SearchMessageCommand}"/>
                            </core:EventTriggerBehavior>
                        </ineractivity:Interaction.Behaviors>
                    </TextBox>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="Sync"
                        Command="{Binding LoadMessagesViewModel.LoadAllMessagesFromServerCommand}"
                        Visibility="{Binding LoginViewModel.ManagmentButtonsVisibility}"/>
                <StackPanel Grid.Column="2"
                            Margin="10,0,0,0">
                    <TextBlock Text="Sort"/>
                    <ComboBox Grid.Column="2">
                        <Button Content="A-Z"
                                Command="{Binding LoadMessagesViewModel.SortByAlphabetMessagesCommand}"/>
                        <Button Content="Z-A"
                                Command="{Binding LoadMessagesViewModel.SortByAlphabetDescendingMessagesCommand}"/>
                        <Button Content="Newer"
                                Command="{Binding LoadMessagesViewModel.SortNewerMessagesCommand}"/>
                        <Button Content="Older"
                                Command="{Binding LoadMessagesViewModel.SortOlderMessagesCommand}"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
            <ListView Margin="10"
                      ItemsSource="{Binding LoadMessagesViewModel.Messages, Mode=TwoWay}"
                      Grid.Row="1"
                      SelectedValue="{Binding ControlMessageViewModel.SelectedMailMessage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <uc:MailMessageControl MailMessage="{Binding}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Flag"
                        Grid.Column="0"
                        Command="{Binding ControlMessageViewModel.FlagMessageCommand}"
                        Visibility="{Binding ControlMessageViewModel.MessageBodyControlsVisibility}"/>
                <Button Content="Delete"
                        Grid.Column="1"
                        Command="{Binding ControlMessageViewModel.DeleteMessageCommand}"
                        Visibility="{Binding ControlMessageViewModel.MessageBodyControlsVisibility}"/>
                <Button Content="X"
                        Grid.Column="2"
                        Background="red"
                        Command="{Binding ControlMessageViewModel.CloseMessageCommand}"
                        Visibility="{Binding ControlMessageViewModel.MessageBodyControlsVisibility}"/>
            </Grid>
            <uc:MessageBodyControl MessageBody="{Binding ControlMessageViewModel.SelectedMailMessage.Body}"
                                   Visibility="{Binding ControlMessageViewModel.MessageBodyControlsVisibility}"
                                   Grid.Row="1"
                                   Margin="10"/>
            <Grid Grid.Row="1"
                  Visibility="{Binding SendMessageViewModel.SendMessageControlsVisibility}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.7*"/>
                    <RowDefinition Height="0.4*"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="0.5*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1.5*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="From"
                                   Margin="10,0,0,0"
                                   Grid.Column="0"
                                   Grid.Row="0"/>
                        <TextBox Margin="5,0,5,0"
                                 Grid.Column="1"
                                 Grid.Row="0"
                                 Text="{Binding SendMessageViewModel.MessageSender}"/>
                        <TextBlock Text="To"
                                   Margin="10,0,0,0"
                                   Grid.Column="0"
                                   Grid.Row="1"/>
                        <TextBox Margin="5,0,5,0"
                                 Grid.Column="1"
                                 Grid.Row="1"
                                 Text="{Binding SendMessageViewModel.MessageReciever, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Margin="10,0,0,0"
                                       Text="Name"/>
                            <TextBox Margin="5"
                                     Text="{Binding SendMessageViewModel.MessageName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Margin="10,0,0,0"
                                       Text="Subject"/>
                            <TextBox Margin="5"
                                     Text="{Binding SendMessageViewModel.MessageSubject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
                <TextBlock Text="Message:"
                           Grid.Row="1"
                           Margin="10,0,0,0"/>
                <TextBox Margin="5,0,5,0"
                         Grid.Row="2"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         Text="{Binding SendMessageViewModel.MessageBody, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Cancel"
                            Grid.Column="0"
                            Margin="5"
                            HorizontalAlignment="Left"
                            Command="{Binding SendMessageViewModel.CancelSendingMessageCommand}"/>
                    <Button Content="Send"
                            Grid.Column="1"
                            Margin="5"
                            HorizontalAlignment="Right"
                            Command="{Binding SendMessageViewModel.SendMessageCommand}"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
